<UserControl x:Class="PackTracker.Presentation.Views.Admin.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MainBackground}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <Border Style="{DynamicResource GlassPanelStyle}" Padding="20" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Admin Overview"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{DynamicResource AccentGoldBrush}"/>
                    <TextBlock Text="High-level system analytics and administrative status."
                               Margin="0,6,0,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <UniformGrid Columns="4" Margin="0,0,0,24">
                <Border Style="{DynamicResource GlassPanelStyle}" Margin="0,0,12,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Authority Level" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding HighestTier}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource GlassPanelStyle}" Margin="6,0,6,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Total Members" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding TotalMembers}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource GlassPanelStyle}" Margin="6,0,6,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Admin Staff" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding ActiveAdminRoleAssignments}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource GlassPanelStyle}" Margin="12,0,0,0" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Audit Volume" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding TotalAuditEntries}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource GlassPanelStyle}" Padding="20" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Infrastructure Status" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource AccentGoldBrush}"/>
                        
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Discord Integration" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <Border HorizontalAlignment="Right" Padding="8,2" CornerRadius="4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DiscordSettingsConfigured}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SuccessFillBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding DiscordSettingsConfigured}" Value="False">
                                                <Setter Property="Background" Value="{DynamicResource DangerFillBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock FontWeight="Bold" FontSize="11">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DiscordSettingsConfigured}" Value="True">
                                                    <Setter Property="Text" Value="CONFIGURED"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DiscordSettingsConfigured}" Value="False">
                                                    <Setter Property="Text" Value="NOT SET"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>

                        <Grid>
                            <TextBlock Text="Core Database" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <Border HorizontalAlignment="Right" Background="{DynamicResource SuccessFillBrush}" Padding="8,2" CornerRadius="4">
                                <TextBlock Text="CONNECTED" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold" FontSize="11"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{DynamicResource GlassPanelStyle}" Padding="20" Margin="12,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Quick Actions" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource AccentGoldBrush}"/>
                        <Button Content="System Health Check" Style="{DynamicResource SecondaryActionButtonStyle}" HorizontalAlignment="Left" Width="200"/>
                        <Button Content="Sync Discord Roles" Style="{DynamicResource SecondaryActionButtonStyle}" HorizontalAlignment="Left" Width="200" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
