<UserControl x:Class="PackTracker.Presentation.Views.Admin.AdminShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1C1714" Padding="20">
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="{Binding AdminTierLabel}" Margin="0,6,0,20" Foreground="#C8A040"/>

                <Button Content="Dashboard" Margin="0,0,0,10" Click="Dashboard_Click"/>
                <Button Content="Settings" Margin="0,0,0,10" Click="Settings_Click"/>
                <Button Content="Members" Margin="0,0,0,10" Click="Members_Click"/>
                <Button Content="Medals" Margin="0,0,0,10" IsEnabled="False"/>
                <Button Content="Operations" Margin="0,0,0,10" IsEnabled="False"/>
                <Button Content="Recruiting" Margin="0,0,0,10" IsEnabled="False"/>
                <Button Content="Audit Logs" Margin="0,0,0,10" IsEnabled="False"/>

                <Separator Margin="0,16,0,16"/>
                <Button Content="Back to PackTracker" Click="Back_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Border Background="#14000000" Margin="20" CornerRadius="12">
                <ContentControl x:Name="AdminContentHost"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
