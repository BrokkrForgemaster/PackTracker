<UserControl x:Class="PackTracker.Presentation.Views.Admin.AdminMembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MainBackground}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{DynamicResource GlassPanelStyle}" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Members Management"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{DynamicResource AccentGoldBrush}"/>
                <TextBlock Text="Assign or revoke PackTracker admin roles for verified clan members."
                           Margin="0,6,0,0"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource GlassPanelStyle}" Padding="16" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Select Role:" 
                               VerticalAlignment="Center" 
                               Margin="0,0,12,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <ComboBox Width="240"
                              ItemsSource="{Binding Roles}"
                              SelectedItem="{Binding SelectedRole}"
                              DisplayMemberPath="Name"
                              VerticalAlignment="Center"/>
                    
                    <Button Content="Assign Role" 
                            Style="{DynamicResource PrimaryActionButtonStyle}"
                            Width="140" 
                            Margin="20,0,0,0" 
                            Click="AssignRole_Click"
                            VerticalAlignment="Center"/>
                    
                    <Button Content="Revoke Role" 
                            Style="{DynamicResource SecondaryActionButtonStyle}"
                            Width="140" 
                            Margin="12,0,0,0" 
                            Click="RevokeRole_Click"
                            VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Column="1" 
                           Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource SuccessBrush}"
                           FontWeight="SemiBold"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{DynamicResource GlassPanelStyle}" Padding="2">
            <DataGrid ItemsSource="{Binding Members}"
                      SelectedItem="{Binding SelectedMember}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="160"/>
                    <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="180"/>
                    <DataGridTextColumn Header="Discord Rank" Binding="{Binding DiscordRank}" Width="140"/>
                    <DataGridTextColumn Header="Join Date" Binding="{Binding JoinDate}" Width="160"/>
                    <DataGridTextColumn Header="Last Login" Binding="{Binding LastLogin}" Width="160"/>
                    <DataGridTextColumn Header="Admin Roles" Binding="{Binding ActiveAdminRoles}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
