<UserControl x:Class="PackTracker.Presentation.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{DynamicResource MainBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20,15" Background="{DynamicResource SidebarBackground}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="OPERATIONAL GUIDE" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="TxtSection" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
                <Button DockPanel.Dock="Right" Content="CLOSE" Click="Close_Click" Style="{DynamicResource SecondaryActionButtonStyle}" Padding="15,8"/>
            </DockPanel>
        </Border>

        <wv2:WebView2 x:Name="WebView" Grid.Row="1" Margin="0"/>
    </Grid>
</UserControl>
