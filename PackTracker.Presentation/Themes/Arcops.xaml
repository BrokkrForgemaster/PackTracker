<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="AccentColor">#169FC8</Color>
    <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource AccentColor}"/>

    <SolidColorBrush x:Key="SidebarBackground" Color="#121A1E"/>
    <SolidColorBrush x:Key="MainBackground"    Color="#0D1114"/>
    <SolidColorBrush x:Key="CardBackground"    Color="#162127"/>

    <BitmapImage x:Key="SidebarImage"
                 UriSource="/Assets/ARCOPS.png"/>

    <DropShadowEffect x:Key="CardShadow"
                      Color="{DynamicResource AccentColor}"
                      ShadowDepth="4"
                      BlurRadius="14"
                      Opacity="0.45"/>

    <DropShadowEffect x:Key="ContentGlowEffect"
                      Color="{DynamicResource AccentColor}"
                      BlurRadius="22"
                      ShadowDepth="0"
                      Opacity="0.5"/>

    <Style x:Key="SidebarBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SidebarBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Effect" Value="{DynamicResource CardShadow}"/>
    </Style>

    <Style x:Key="ContentBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{DynamicResource AccentColor}"
                                  BlurRadius="26"
                                  ShadowDepth="0"
                                  Opacity="0.38"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
