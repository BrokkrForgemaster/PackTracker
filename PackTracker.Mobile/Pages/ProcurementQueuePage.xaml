<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PackTracker.Mobile.Pages.ProcurementQueuePage"
             Title="Procurement Center">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">
            <Label Text="PROCUREMENT CENTER" Style="{StaticResource TitleLabelStyle}" />
            <Border Style="{StaticResource CardBorderStyle}">
                <Label Text="No procurement requests." Style="{StaticResource MutedLabelStyle}" />
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
